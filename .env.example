PORT=3007
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority
ACCESS_TOKEN_SECRET=<generate with: node -e "require('crypto').randomBytes(64).toString('hex')">
REFRESH_TOKEN_SECRET=<generate with: node -e "require('crypto').randomBytes(64).toString('hex')">